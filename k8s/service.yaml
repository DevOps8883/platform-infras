# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: nexacloud-service
  namespace: nexacloud
spec:
  type: LoadBalancer
  selector:
    app: nexacloud-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  
