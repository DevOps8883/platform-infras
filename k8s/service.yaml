# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: nexacloud-service
  namespace: nexacloud
spec:
  selector:
    app: nexacloud
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: NodePort
